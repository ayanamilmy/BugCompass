# 示例：修改属性后视图未立即更新

在某个对象的自定义属性发生变化后，3D 视图仍显示旧结果；切换到另一个对象再切回时才刷新。

## 预期行为

修改属性后，依赖该属性的显示结果应立即更新。

## 实际行为

属性面板显示了新值，但 3D 视图保持旧结果。

## 复现步骤

1. 打开包含相关对象的文件。
2. 修改对象的自定义属性。
3. 观察 3D 视图。

版本、操作系统、最小示例文件以及具体属性路径尚未提供。
